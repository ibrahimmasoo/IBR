EMAIL SETUP FOR CONTACT FORM
============================

The contact form now:
1) saves each message in Supabase table: public.contact_messages
2) calls the Edge Function: send-contact-email
3) the Edge Function sends email to: ibrahimmasoud201@gmail.com

YOU STILL NEED TO DO THESE 4 STEPS IN SUPABASE:

1. Run the SQL file:
   supabase-contact-policies.sql

2. Create / deploy the Edge Function:
   Folder included:
   supabase/functions/send-contact-email/index.ts

3. In Supabase, add secret:
   RESEND_API_KEY = your Resend API key

4. Deploy the function name exactly as:
   send-contact-email

After deployment, the current website code will call:
https://tgbgeobkdolflxkzmtxf.functions.supabase.co/send-contact-email

If email is not configured yet, the site will still save the message in the dashboard.